public class AudioEnum {
    // AudioSourceの列挙
    public enum Enm_AudSrc {
        BGM,
        SE_Chara,
        SE_UI,
    }

    // BGM
    public enum Enm_BGM {
        bgm1,
    }

    // SE_Chara
    public enum Enm_SE_Chara {
        jump,
    }

    // SE_UI
    public enum Enm_SE_UI {
        click,
        celect,
        cancel,
        pico,
        caution,
        clear,
    }
}
