public class AudioEnum {
    // AudioSourceの列挙
    public enum AudSrc {
        BGM,
        SE_Cat,
        SE_Fish,
        SE_UI,
    }

    // BGM
    public enum BGM {
        bgm1,
    }

    // 猫
    public enum SE_Cat {
        jump,
    }

    // 魚
    public enum SE_Fish {
        eaten,
    }

    // UI
    public enum SE_UI {
        click,
        celect,
        cancel,
        pico,
        caution,
        clear,
    }
}
